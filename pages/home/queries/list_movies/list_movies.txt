SELECT
  *
FROM
  public.movies
WHERE
  "title" ilike '%{{movies_table.searchText}}%'
ORDER BY
  "{{movies_table.sortOrder.column || 'id'}}" {{movies_table.sortOrder.order !== "desc" ? "" : "DESC"}}
LIMIT
  {{movies_table.pageSize}}
OFFSET
  {{movies_table.pageOffset}}